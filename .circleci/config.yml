jobs:
  build:
    docker:
      - image: 'node:latest'
    steps:
      - checkout
      - run:
          name: test
          command: |
            sonarqube/analyze
orbs:
  sonarqube: clicklogiq/sonarqube@1.0.1
version: 2.1
workflows:
  main:
    jobs:
      - build:
          context: sonarcloud
